services:
  database:
    container_name: "postgres-dev"
    image: "postgres:17.2-alpine3.21"
    env_file:
      - ../.env.development
    ports:
      - "5432:5432"
    healthcheck:
      interval: 5s
      timeout: 5s
      retries: 5